package com.sigsauer.univ.whenmybday.config.binder;

import org.glassfish.hk2.utilities.binding.AbstractBinder;

import javax.inject.Singleton;

public class ApplicationBinder extends AbstractBinder {

    @Override
    protected void configure() {
        /* implementation of interface | interface */
//        bind(UserServiceImpl.class).to(UserService.class).in(Singleton.class);
//        bind(UserRepositoryImpl.class).to(UserRepository.class).in(Singleton.class);
//        bind(AuthenticationServiceImpl.class).to(AuthenticationService.class).in(Singleton.class);
//        bind(ConversationRepositoryImpl.class).to(ConversationRepository.class).in(Singleton.class);
//        bind(ConversationServiceImpl.class).to(ConversationService.class).in(Singleton.class);
//        bind(MessageRepositoryImpl.class).to(MessageRepository.class).in(Singleton.class);
//        bind(MessageServiceImpl.class).to(MessageService.class).in(Singleton.class);
    }
}
